<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.boardver56.model.dao.BoardMapper">

    <!-- ===== ResultMap 정의 ===== -->
    <resultMap id="boardResultMap" type="com.abc.boardver56.model.dto.BoardDTO">
        <id property="boardId" column="board_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="userActive" column="user_active"/>
        <result property="adminActive" column="admin_active"/>
        <result property="sortOrder" column="sort_order"/>
    </resultMap>

    <resultMap id="postResultMap" type="com.abc.boardver56.model.dto.PostDTO">
        <id property="postId" column="post_id"/>
        <result property="boardId" column="board_id"/>
        <result property="authorId" column="author_id"/>
        <result property="password" column="password"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="viewCount" column="view_count"/>
        <result property="ipHash" column="ip_hash"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="attachmentUrl" column="attachment_url"/>
        <result property="thumbnailUrl" column="thumbnail_url"/>
        <result property="accessLevel" column="access_level"/>
        <result property="status" column="status"/>
        <result property="notice" column="is_notice"/>
        <result property="secret" column="is_secret"/>
        <result property="old" column="old" />
        <result property="category" column="category" />
    </resultMap>

    <resultMap id="commentResultMap" type="com.abc.boardver56.model.dto.CommentDTO">
        <id property="commentId" column="comment_id"/>
        <result property="postId" column="post_id"/>
        <result property="authorId" column="author_id"/>
        <result property="password" column="password"/>
        <result property="content" column="content"/>
        <result property="ipHash" column="ip_hash"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- ===== Board ===== -->
    <select id="findAllBoards" resultMap="boardResultMap">
        SELECT board_id, name, description, user_active, admin_active, sort_order
        FROM board
        ORDER BY sort_order ASC
    </select>

    <select id="findOneBoard" resultMap="boardResultMap">
        SELECT board_id, name, description, user_active, admin_active, sort_order
        FROM board
        WHERE board_id = #{boardId}
    </select>

    <insert id="addBoard" parameterType="com.abc.boardver56.model.dto.BoardDTO"
            useGeneratedKeys="true" keyProperty="boardId">
        INSERT INTO board (name, description, user_active, admin_active, sort_order)
        VALUES (#{name}, #{description}, #{userActive}, #{adminActive}, #{sortOrder})
    </insert>

    <update id="updateBoard" parameterType="com.abc.boardver56.model.dto.BoardDTO">
        UPDATE board
        SET name = #{name},
        description = #{description},
        user_active = #{userActive},
        admin_active = #{adminActive}
        WHERE board_id = #{boardId}
    </update>

    <delete id="deleteBoard" parameterType="com.abc.boardver56.model.dto.BoardDTO">
        DELETE FROM board WHERE board_id = #{boardId}
    </delete>



    <!-- ===== Post ===== -->
    <select id="findAllPosts" resultMap="postResultMap">
        SELECT post_id, board_id, author_id, password, title, content,
        view_count, ip_hash, created_at, updated_at,
        attachment_url, thumbnail_url, access_level, status,
        is_notice, is_secret, old, category
        FROM post
        ORDER BY post_id
    </select>

    <select id="findOnePost" resultMap="postResultMap">
        SELECT post_id, board_id, author_id, password, title, content,
        view_count, ip_hash, created_at, updated_at,
        attachment_url, thumbnail_url, access_level, status,
        is_notice, is_secret, old, category
        FROM post
        WHERE post_id = #{postId}
    </select>

    <!-- 1. addPost 수정 -->
    <insert id="addPost" parameterType="com.abc.boardver56.model.dto.PostDTO">
        INSERT INTO post (
        board_id, author_id, password, ip_hash,
        attachment_url, title, content,
        thumbnail_url, status, is_notice, is_secret, old, category
        )
        VALUES (
        #{boardId}, #{authorId}, #{password}, #{ipHash},
        #{attachmentUrl}, #{title}, #{content},
        #{thumbnailUrl}, #{status}, #{notice}, #{secret}, 0, #{category}
        )
    </insert>

    <!-- 2. updatePost 수정 -->
    <update id="updatePost" parameterType="com.abc.boardver56.model.dto.PostDTO">
        UPDATE post
        SET board_id = #{boardId},
        author_id = #{authorId},
        password = #{password},
        ip_hash = #{ipHash},
        attachment_url = #{attachmentUrl},
        title = #{title},
        content = #{content},
        thumbnail_url = #{thumbnailUrl},
        access_level = #{accessLevel},
        status = #{status},
        view_count = #{viewCount},
        created_at = #{createdAt},
        updated_at = #{updatedAt},
        is_notice = #{notice},
        is_secret = #{secret},
        old = #{old},
        category = #{category}
        WHERE post_id = #{postId}
    </update>

    <!-- 3. searchPosts 수정 (resultMap 사용 추천) -->
    <select id="searchPosts" resultMap="postResultMap">
        SELECT post_id, board_id, author_id, password, title, content, view_count,
        ip_hash, created_at, updated_at, attachment_url, thumbnail_url,
        access_level, status, is_notice, is_secret, old, category
        FROM post
        WHERE board_id = #{boardId}
        AND (title LIKE CONCAT('%', #{keyword}, '%')
        OR author_id LIKE CONCAT('%', #{keyword}, '%')
        OR content LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY post_id DESC
    </select>
    <delete id="deletePost" parameterType="com.abc.boardver56.model.dto.PostDTO">
        DELETE FROM post
        WHERE post_id = #{postId}
        AND author_id = #{authorId}
        AND password = #{password}
    </delete>

    <select id="findPostsByBoardId" resultMap="postResultMap">
        SELECT post_id, board_id, author_id, password, title, content,
        view_count, ip_hash, created_at, updated_at,
        attachment_url, thumbnail_url, access_level, status,
        is_notice, is_secret, old, category
        FROM post
        WHERE board_id = #{boardId}
        ORDER BY post_id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 총 글 개수 구하기 -->
    <select id="countPostsByBoardId" resultType="int">
        SELECT COUNT(*)
        FROM post
        WHERE board_id = #{boardId}
    </select>


    <!-- ===== Comment ===== -->
    <select id="findAllComments" resultMap="commentResultMap">
        SELECT comment_id, post_id, author_id, password,
        content, ip_hash, created_at, updated_at
        FROM comment
        ORDER BY comment_id
    </select>

    <select id="findOneComment" resultMap="commentResultMap">
        SELECT comment_id, post_id, author_id, password,
        content, ip_hash, created_at, updated_at
        FROM comment
        WHERE comment_id = #{commentId}
    </select>

    <insert id="addComment" parameterType="com.abc.boardver56.model.dto.CommentDTO">
        INSERT INTO comment (
        post_id, author_id, password, content, ip_hash
        )
        VALUES (
        #{postId}, #{authorId}, #{password}, #{content}, #{ipHash}
        )
    </insert>

    <update id="updateComment" parameterType="com.abc.boardver56.model.dto.CommentDTO">
        UPDATE comment
        SET post_id = #{postId},
        author_id = #{authorId},
        password = #{password},
        content = #{content},
        ip_hash = #{ipHash}
        WHERE comment_id = #{commentId}
    </update>

    <delete id="deleteComment" parameterType="com.abc.boardver56.model.dto.CommentDTO">
        DELETE FROM comment
        WHERE comment_id = #{commentId}
    </delete>


    <update id="increaseViewCount">
        UPDATE post
        SET view_count = view_count + 1
        WHERE post_id = #{postId}
    </update>


</mapper>
