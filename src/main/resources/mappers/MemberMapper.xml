<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.abc.boardver56.model.dao.MemberMapper">

    <insert id="insertMember" parameterType="com.abc.boardver56.model.dto.MemberDto">
        INSERT INTO member (username, password, name, email, phone, role, status)
        VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{role}, #{status})
    </insert>

    <select id="findByUsername" parameterType="string" resultType="com.abc.boardver56.model.dto.MemberDto">
        SELECT
        member_id AS memberId,
        username,
        password,
        name,
        email,
        phone,
        role,
        status,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM member
        WHERE username = #{username}
    </select>

</mapper>